import 'package:flutter/material.dart';

class MinbarScaffold extends StatelessWidget {
  const MinbarScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
